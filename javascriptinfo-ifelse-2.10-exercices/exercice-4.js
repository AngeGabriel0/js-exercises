// Réécrivez ce if en utilisant l’opérateur conditionnel '?' :

let a = 1;
let b = 2;

if (a + b < 4) {
    result = 'Below';
} else {
    result = 'Over';
}

//Réponse en ternaire 
let resultTernaire = a + b < 4 ? 'Below' : 'Over';
console.log(resultTernaire);